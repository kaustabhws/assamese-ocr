# Fonts

Run `python scripts/download_fonts.py` to download OFL-licensed fonts that cover the shared Eastern Nagari script. Synthetic labels remain Assamese-only, and rendering requests Assamese language shaping when supported.

Downloaded font binaries are ignored by version control. Their source URLs and license links are written to `font_manifest.json`.

